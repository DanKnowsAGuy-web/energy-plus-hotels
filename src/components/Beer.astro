---
// Power-quality, explained as a foamy pour. Scroll-driven: the scene pins and
// each scroll step advances one cost; scrolling back reverses it. A tap pours
// the "load" into the glass: a chaotic, nonlinear pour throws foam (the four
// costs); a clean, linear pour fills the glass with useful energy. The glass
// and the utility bill animate in lockstep so each cost lights up on both.
// Bill figures are illustrative.
const steps = [
  {
    n: "", key: "intro", title: "How it used to flow",
    body: "Power arrived as one smooth, even pour, almost all of it useful.",
  },
  {
    n: "01", key: "foam", title: "The foam",
    body: "Modern equipment pulls power in pulses, not a smooth draw, so you're billed for “foam” you can't use.",
  },
  {
    n: "02", key: "spills", title: "The spills",
    body: "Some slops over the side, useful energy lost as heat in your wires and motors.",
  },
  {
    n: "03", key: "mess", title: "The mess",
    body: "It leaves a mess on the bar, and the utility adds a penalty to your bill.",
  },
  {
    n: "04", key: "mug", title: "The mug",
    body: "The rough pour is hard on the glass. Your breakers and transformers wear out years early.",
  },
];

---

<section class="section pq" id="power" aria-labelledby="pq-title">
  <div class="container">
    <div class="pq__head reveal">
      <p class="pq__kicker">So what does the exam find?</p>
      <h2 class="pq__title" id="pq-title">
        You ordered the full beer. Why pay for the foam?
      </h2>
    </div>
  </div>

  <div class="pq__track" id="pq-track">
    <div class="pq__sticky">
      <div class="container pq__stage" id="pq-stage" data-active="intro">
        <div class="pq__cards" id="pq-cards">
          {steps.map((s, i) => (
            <article class="pq__card" data-card={s.key} data-i={i}>
              <h3 class="pq__card-title">
                {s.n && <span class="pq__card-num">{s.n}</span>}{s.title}
              </h3>
              <p class="pq__card-body">{s.body}</p>
            </article>
          ))}
          <article class="pq__card pq__card--clean" data-card="clean" data-i={steps.length}>
            <h3 class="pq__card-title">
              <span class="pq__card-num">&#10003;</span>A cleaner pour
            </h3>
            <p class="pq__card-body">
              Harmonize the power and the same bill fills a full, useful mug.
              No foam, no spills, no mess, no worn-out glass.
            </p>
          </article>
        </div>

        <div class="pq__cols">
          <div class="pq__col pq__glass-col">
            <div class="pq__glass-row">
              <button class="pq__nav pq__nav--prev" type="button" aria-label="Previous pour">&lsaquo;</button>
            <svg class="pq__glass" viewBox="0 0 240 440" role="img"
              aria-label="A tap pours into a tall glass. A chaotic pour throws a thick head of foam and spills down the side; a clean, even pour fills the glass with useful beer.">
              <!-- tap -->
              <g class="pq__tap">
                <rect x="22" y="24" width="98" height="13" rx="4"></rect>
                <rect x="58" y="10" width="12" height="16" rx="3"></rect>
                <rect x="106" y="24" width="22" height="46" rx="4"></rect>
              </g>

              <!-- the pour: chaotic (nonlinear) vs smooth (linear) -->
              <g class="pq__stream pq__stream--chaotic">
                <path class="pq__jet" d="M117 70 q-12 26 3 50 q13 22 -4 46 q-9 14 2 22"></path>
                <circle class="pq__splat" cx="104" cy="120" r="3.4"></circle>
                <circle class="pq__splat" cx="132" cy="150" r="2.8"></circle>
                <circle class="pq__splat" cx="112" cy="168" r="3"></circle>
              </g>
              <g class="pq__stream pq__stream--smooth">
                <rect class="pq__jet-smooth" x="113" y="70" width="9" height="118" rx="4"></rect>
              </g>

              <!-- spill drips + puddle -->
              <g class="pq__part" data-part="spills">
                <path class="pq__drip" d="M168 196 q7 36 2 70"></path>
                <path class="pq__drip" d="M74 214 q-7 28 -1 52"></path>
              </g>
              <g class="pq__part" data-part="mess">
                <ellipse class="pq__puddle" cx="120" cy="418" rx="74" ry="9"></ellipse>
              </g>

              <!-- liquid, clipped to glass interior -->
              <clipPath id="pq-clip">
                <path d="M70 186 L170 186 L156 404 L84 404 Z"></path>
              </clipPath>
              <g clip-path="url(#pq-clip)">
                <rect class="pq__beer" x="64" y="262" width="112" height="150"></rect>
                <rect class="pq__beer-top" x="64" y="190" width="112" height="90"></rect>
                <g class="pq__part" data-part="foam">
                  <rect class="pq__foam" x="64" y="190" width="112" height="78"></rect>
                  <circle class="pq__bub" cx="92" cy="218" r="10" style="--bd:0s"></circle>
                  <circle class="pq__bub" cx="128" cy="208" r="8" style="--bd:-1.1s"></circle>
                  <circle class="pq__bub" cx="110" cy="242" r="7" style="--bd:-2.2s"></circle>
                  <circle class="pq__bub" cx="146" cy="232" r="6" style="--bd:-0.6s"></circle>
                  <circle class="pq__bub" cx="80" cy="246" r="6" style="--bd:-1.7s"></circle>
                  <circle class="pq__bub" cx="156" cy="216" r="5" style="--bd:-2.8s"></circle>
                </g>
              </g>

              <!-- the mug itself -->
              <g class="pq__part" data-part="mug">
                <path class="pq__glass-out" d="M70 186 L170 186 L156 404 L84 404 Z"></path>
                <line class="pq__rim" x1="66" y1="186" x2="174" y2="186"></line>
              </g>
            </svg>
              <button class="pq__nav pq__nav--next" type="button" aria-label="Next pour">&rsaquo;</button>
            </div>
            <span class="pq__load" id="pq-load">Nonlinear load</span>
          </div>

          <div class="pq__col pq__bill-col">
            <div class="pq__bill" id="pq-bill">
              <div class="pq__bill-head">
                <span class="pq__bill-name">Utility Statement</span>
              </div>
              <div class="pq__bill-active" id="pq-bill-active" data-kind="useful">
                <span class="pq__bill-active-label" id="pq-bill-label">Useful energy</span>
                <span class="pq__bill-active-amt" id="pq-bill-amt">$1,000</span>
              </div>
              <div class="pq__bill-total">
                <span>Total</span>
                <span class="pq__total" id="pq-total">$1,000</span>
              </div>
            </div>
          </div>
        </div>

        <div class="pq__dots" id="pq-dots" role="tablist" aria-label="Power-quality costs">
          {steps.map((s, i) => (
            <button class="pq__dot" data-go={i} aria-label={s.title} type="button"></button>
          ))}
          <button class="pq__dot pq__dot--clean" data-go={steps.length} aria-label="A cleaner pour" type="button"></button>
        </div>
      </div>
    </div>
  </div>

  <div class="container">
    <p class="pq__close reveal">
      A cleaner pour fixes all four at once. Cut the distortion and the same bill
      buys a full mug of useful energy.
    </p>
  </div>
</section>

<script is:inline>
  (() => {
    const stage = document.getElementById("pq-stage");
    const track = document.getElementById("pq-track");
    if (!stage || !track) return;
    const reduce = matchMedia("(prefers-reduced-motion: reduce)").matches;
    const cardsWrap = document.getElementById("pq-cards");
    const cards = Array.from(stage.querySelectorAll(".pq__card"));
    const dots = Array.from(stage.querySelectorAll(".pq__dot"));
    let prevShown = 0;
    const totalEl = document.getElementById("pq-total");
    const loadEl = document.getElementById("pq-load");
    const billRow = document.getElementById("pq-bill-active");
    const labelEl = document.getElementById("pq-bill-label");
    const amtEl = document.getElementById("pq-bill-amt");
    const order = ["intro", "foam", "spills", "mess", "mug", "clean"];
    const STEPS = order.length;

    // each step shows one charge on the meter, plus the running total
    const billInfo = {
      intro: { label: "Useful energy", amt: 1000, kind: "useful" },
      foam: { label: "Power you can't use", amt: 140, kind: "waste" },
      spills: { label: "Energy lost as heat", amt: 95, kind: "waste" },
      mess: { label: "Utility penalty", amt: 70, kind: "waste" },
      mug: { label: "Early equipment wear", amt: 55, kind: "waste" },
      clean: { label: "Energy harmonization", amt: 360, kind: "save" },
    };
    const totalByIdx = [1000, 1140, 1235, 1305, 1360, 1000];
    const PEAK = 1360;
    const fmt = (n) => "$" + Math.round(n).toLocaleString();
    const signed = (info) =>
      (info.kind === "waste" ? "+$" : info.kind === "save" ? "−$" : "$") +
      info.amt.toLocaleString();

    let totalTimer = null;
    const countTo = (target) => {
      if (totalTimer) cancelAnimationFrame(totalTimer);
      const start = parseInt(totalEl.textContent.replace(/[^0-9]/g, ""), 10) || BASE;
      const t0 = performance.now();
      const dur = 500;
      const tick = (now) => {
        const p = Math.min((now - t0) / dur, 1);
        const e = 1 - Math.pow(1 - p, 3);
        totalEl.textContent = fmt(start + (target - start) * e);
        if (p < 1) totalTimer = requestAnimationFrame(tick);
      };
      totalTimer = requestAnimationFrame(tick);
    };

    const pulse = (el) => {
      if (!el) return;
      el.classList.remove("is-just");
      void el.offsetWidth; // restart the animation
      el.classList.add("is-just");
    };

    const show = (idx, animate) => {
      const key = order[idx];
      cardsWrap.dataset.dir = idx >= prevShown ? "fwd" : "back";
      prevShown = idx;
      stage.dataset.active = key;
      loadEl.textContent = (key === "clean" || key === "intro") ? "Linear load" : "Nonlinear load";
      cards.forEach((c) => c.classList.toggle("is-active", Number(c.dataset.i) === idx));
      dots.forEach((d) => d.classList.toggle("is-active", Number(d.dataset.go) === idx));

      // the meter swaps to the charge being illustrated, total follows
      const info = billInfo[key];
      labelEl.textContent = info.label;
      amtEl.textContent = signed(info);
      billRow.dataset.kind = info.kind;
      const target = totalByIdx[idx];
      animate ? countTo(target) : (totalEl.textContent = fmt(target));

      // flash what just changed, to draw the eye
      if (animate) {
        if (key !== "intro" && key !== "clean") {
          pulse(stage.querySelector(`.pq__part[data-part="${key}"]`));
        }
        pulse(billRow);
        pulse(totalEl);
      }
    };

    // Static fallback (reduced motion or no IntersectionObserver): no pin,
    // all cards listed, meter parked on the summed losses.
    if (reduce || !("IntersectionObserver" in window)) {
      stage.classList.add("is-static");
      track.classList.add("is-static");
      cards.forEach((c) => c.classList.add("is-active"));
      labelEl.textContent = "Power-quality losses";
      amtEl.textContent = "+$360";
      billRow.dataset.kind = "waste";
      totalEl.textContent = fmt(PEAK);
      stage.dataset.active = "all";
      dots.forEach((d) =>
        d.addEventListener("click", () => show(Number(d.dataset.go), true)),
      );
      return;
    }

    // MOBILE: tap / swipe to advance, no scroll pin (scroll-jacking fights the
    // address bar on phones). The scene is a fixed-height panel in normal flow;
    // the reader taps, swipes, or uses the dots to walk card to card.
    const mobile = matchMedia("(max-width: 640px)").matches;
    if (mobile) {
      stage.classList.add("is-tap");
      const prevBtn = stage.querySelector(".pq__nav--prev");
      const nextBtn = stage.querySelector(".pq__nav--next");
      let i = 0;
      // no left arrow on the first card, no right arrow on the last (no loop)
      const updateArrows = () => {
        prevBtn.classList.toggle("is-off", i === 0);
        nextBtn.classList.toggle("is-off", i === STEPS - 1);
      };
      const go = (n) => {
        i = Math.max(0, Math.min(STEPS - 1, n));
        show(i, true);
        updateArrows();
      };
      show(0, false);
      updateArrows();

      // Advance ONLY on a push: arrows, a tap on the scene, swipe, or the dots.
      // No automated timer; the per-card animations still play on each push.
      dots.forEach((d) =>
        d.addEventListener("click", (e) => { e.stopPropagation(); go(Number(d.dataset.go)); }),
      );
      nextBtn.addEventListener("click", (e) => { e.stopPropagation(); go(i + 1); });
      prevBtn.addEventListener("click", (e) => { e.stopPropagation(); go(i - 1); });
      stage.addEventListener("click", () => go(i + 1));
      // horizontal swipe: left = next, right = back
      let x0 = null;
      stage.addEventListener("touchstart", (e) => { x0 = e.touches[0].clientX; }, { passive: true });
      stage.addEventListener("touchend", (e) => {
        if (x0 === null) return;
        const dx = e.changedTouches[0].clientX - x0;
        if (Math.abs(dx) > 40) go(dx < 0 ? i + 1 : i - 1);
        x0 = null;
      }, { passive: true });
      return;
    }

    // DESKTOP: scroll-driven. Map progress through the pinned track to a step.
    track.classList.add("is-pinned");
    let current = -1;
    let ticking = false;
    const measure = () => {
      const span = track.offsetHeight - window.innerHeight;
      const passed = Math.min(Math.max(-track.getBoundingClientRect().top, 0), span);
      const p = span > 0 ? passed / span : 0;
      const idx = Math.max(0, Math.min(STEPS - 1, Math.floor(p * STEPS - 1e-6)));
      if (idx !== current) {
        current = idx;
        show(idx, true);
      }
    };
    addEventListener(
      "scroll",
      () => {
        if (!ticking) {
          ticking = true;
          requestAnimationFrame(() => {
            measure();
            ticking = false;
          });
        }
      },
      { passive: true },
    );
    addEventListener("resize", measure, { passive: true });

    // Dots scroll the page to the matching step.
    dots.forEach((d) =>
      d.addEventListener("click", () => {
        const span = track.offsetHeight - window.innerHeight;
        const idx = Number(d.dataset.go);
        const y = track.offsetTop + ((idx + 0.5) / STEPS) * span;
        scrollTo({ top: y, behavior: "smooth" });
      }),
    );

    show(0, false);
    measure();
  })();
</script>

<style>
  .pq {
    border-top: 1px solid var(--line);
    background:
      radial-gradient(ellipse 55% 35% at 88% 0%, rgba(194, 163, 104, 0.055), transparent 70%),
      var(--bg);
  }
  .pq__head { max-width: 60rem; }
  .pq__kicker {
    font-family: var(--font-mono);
    font-size: var(--step--1);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--gold-text);
    margin-bottom: var(--space-s);
  }
  .pq__title { font-size: var(--step-3); max-width: 22ch; }

  /* scroll track: tall so the pinned scene has room to step through */
  .pq__track.is-pinned { height: 420vh; }
  .pq__track.is-static { height: auto; }
  .pq__track.is-pinned .pq__sticky {
    position: sticky;
    top: 0;
    height: 100vh;
    height: 100svh;
    display: flex;
    align-items: center;
    padding-block: var(--space-m);
  }
  .pq__stage { width: 100%; }
  .pq__stage:not(.is-static) { margin-top: 0; }
  .pq__track.is-static .pq__stage { margin-top: var(--space-xl); }

  /* narration cards: one at a time, stacked in place */
  /* all cards share one grid cell: the box auto-sizes to the tallest,
     no fixed height to overflow, and they cross-slide in place */
  .pq__cards {
    display: grid;
    margin-bottom: var(--space-m);
  }
  .pq__card {
    grid-area: 1 / 1;
    background: rgba(255, 255, 255, 0.022);
    border: 1px solid var(--line-strong);
    border-radius: 12px;
    padding: 1rem 1.3rem;
    opacity: 0;
    transform: translateX(46px);
    transition: opacity 0.4s ease, transform 0.55s var(--ease-out-quart);
    pointer-events: none;
  }
  .pq__card.is-active { opacity: 1; transform: translateX(0); }
  /* scroll back and the cards slide in from the other side */
  .pq__cards[data-dir="back"] .pq__card { transform: translateX(-46px); }
  .pq__cards[data-dir="back"] .pq__card.is-active { transform: translateX(0); }
  .pq__card-num {
    display: inline-block;
    margin-right: 0.55em;
    font-family: var(--font-mono);
    font-size: 0.6em;
    letter-spacing: 0.1em;
    vertical-align: middle;
    color: var(--gold-text);
  }
  .pq__card--clean .pq__card-num { color: var(--mint); }
  .pq__card-title { font-size: var(--step-2); letter-spacing: -0.01em; }
  .pq__card-body {
    margin-top: var(--space-2xs);
    font-size: var(--step-1);
    line-height: 1.5;
    color: var(--body);
    max-width: 46ch;
  }
  .pq__stage.is-static .pq__cards { display: block; }
  .pq__stage.is-static .pq__card {
    opacity: 1;
    transform: none;
    margin-bottom: var(--space-m);
  }

  /* card, beer, and bill stay together and side by side at every width */
  .pq__cols {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(0, 1.18fr);
    gap: clamp(0.75rem, 3vw, 2.5rem);
    align-items: end;
  }
  .pq__col { margin: 0; display: flex; flex-direction: column; min-width: 0; }
  .pq__glass-col { align-items: center; position: relative; }

  /* the load label floats over the pour, clear of the narration cards */
  .pq__load {
    position: absolute;
    top: 30%;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    font-family: var(--font-mono);
    font-size: var(--step--2);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #e0894a;
    border: 1px solid currentColor;
    border-radius: 999px;
    padding: 0.3em 0.85em;
    background: rgba(12, 26, 20, 0.86);
    white-space: nowrap;
    transition: color 0.5s ease;
  }
  .pq__stage[data-active="clean"] .pq__load,
  .pq__stage[data-active="intro"] .pq__load { color: var(--mint); }

  .pq__glass {
    width: 100%;
    max-width: 220px;
    max-height: 46vh;
    height: auto;
    display: block;
    margin-inline: auto;
  }

  .pq__tap { fill: var(--surface-2); stroke: var(--line-strong); stroke-width: 1.5; }

  /* the pour */
  .pq__stream { transition: opacity 0.5s ease; }
  .pq__stream--smooth { opacity: 0; }
  .pq__jet {
    fill: none;
    stroke: var(--gold);
    stroke-width: 6;
    stroke-linecap: round;
    opacity: 0.9;
  }
  .pq__splat { fill: var(--gold); opacity: 0.8; }
  .pq__jet-smooth { fill: var(--gold-text); opacity: 0.92; }
  .pq__stage[data-active="clean"] .pq__stream--chaotic,
  .pq__stage[data-active="intro"] .pq__stream--chaotic { opacity: 0; }
  .pq__stage[data-active="clean"] .pq__stream--smooth,
  .pq__stage[data-active="intro"] .pq__stream--smooth { opacity: 1; }

  /* spotlight: dim every part, brighten the active one */
  .pq__part { transition: opacity 0.45s ease; }
  .pq__stage[data-active] .pq__part { opacity: 0.3; }
  .pq__stage[data-active="foam"] .pq__part[data-part="foam"],
  .pq__stage[data-active="spills"] .pq__part[data-part="spills"],
  .pq__stage[data-active="mess"] .pq__part[data-part="mess"],
  .pq__stage[data-active="mug"] .pq__part[data-part="mug"],
  .pq__stage[data-active="all"] .pq__part { opacity: 1; }

  .pq__beer { fill: var(--gold); }
  .pq__beer-top { fill: var(--gold); opacity: 0; transition: opacity 0.6s ease; }
  .pq__foam { fill: #e3d8c0; }
  .pq__bub {
    fill: #fffaf0;
    stroke: rgba(120, 100, 60, 0.45);
    stroke-width: 1.3;
  }
  .pq__drip { fill: none; stroke: var(--gold); stroke-width: 3.5; stroke-linecap: round; opacity: 0.75; }
  .pq__puddle { fill: var(--gold); opacity: 0.5; }
  .pq__glass-out { fill: none; stroke: var(--line-strong); stroke-width: 2.4; }
  .pq__rim { stroke: var(--line-strong); stroke-width: 3.5; stroke-linecap: round; }
  .pq__stage[data-active="mug"] .pq__glass-out,
  .pq__stage[data-active="mug"] .pq__rim { stroke: #e0894a; }

  /* clean / before: foam gone, beer to the rim, spill + puddle wiped, glass crisp */
  .pq__stage[data-active="clean"] .pq__part[data-part="foam"],
  .pq__stage[data-active="clean"] .pq__part[data-part="spills"],
  .pq__stage[data-active="clean"] .pq__part[data-part="mess"],
  .pq__stage[data-active="intro"] .pq__part[data-part="foam"],
  .pq__stage[data-active="intro"] .pq__part[data-part="spills"],
  .pq__stage[data-active="intro"] .pq__part[data-part="mess"] { opacity: 0; }
  .pq__stage[data-active="clean"] .pq__beer-top,
  .pq__stage[data-active="intro"] .pq__beer-top { opacity: 1; }
  .pq__stage[data-active="clean"] .pq__glass-out,
  .pq__stage[data-active="clean"] .pq__rim,
  .pq__stage[data-active="intro"] .pq__glass-out,
  .pq__stage[data-active="intro"] .pq__rim { stroke: var(--gold-text); opacity: 1; }
  .pq__stage[data-active="clean"] .pq__part,
  .pq__stage[data-active="intro"] .pq__part { opacity: 1; }

  @media (prefers-reduced-motion: no-preference) {
    .pq__bub {
      animation: pqRise 3.4s ease-in-out infinite;
      animation-delay: var(--bd, 0s);
    }
    @keyframes pqRise {
      0% { transform: translateY(4px); opacity: 0.55; }
      50% { transform: translateY(-5px); opacity: 1; }
      100% { transform: translateY(4px); opacity: 0.55; }
    }
    .pq__stream--chaotic .pq__jet { animation: pqJet 0.5s ease-in-out infinite alternate; }
    @keyframes pqJet {
      from { transform: translateX(-3px) scaleY(0.98); }
      to { transform: translateX(3px) scaleY(1.02); }
    }
    .pq__splat { animation: pqSplat 0.9s ease-in infinite; }
    .pq__splat:nth-of-type(3) { animation-delay: -0.3s; }
    .pq__splat:nth-of-type(4) { animation-delay: -0.6s; }
    @keyframes pqSplat {
      0% { transform: translate(0, -8px); opacity: 0; }
      30% { opacity: 0.9; }
      100% { transform: translate(6px, 14px); opacity: 0; }
    }
    .pq__stage[data-active="clean"] .pq__splat,
    .pq__stage[data-active="intro"] .pq__splat { animation: none; opacity: 0; }

    /* on each step change, flash what just changed */
    .pq__part.is-just { animation: pqPartPulse 0.7s ease; }
    @keyframes pqPartPulse {
      0%, 100% { filter: none; }
      40% { filter: drop-shadow(0 0 7px rgba(224, 137, 74, 0.85)) brightness(1.35); }
    }
    .pq__bill-active.is-just { animation: pqLineFlash 0.75s ease; }
    @keyframes pqLineFlash {
      0% { background: rgba(224, 137, 74, 0.2); }
      100% { background: transparent; }
    }
    .pq__total.is-just { animation: pqTotalPop 0.5s ease; }
    @keyframes pqTotalPop {
      0%, 100% { transform: scale(1); }
      45% { transform: scale(1.13); }
    }

    /* the next arrow pulses subtly so it reads as "push me" */
    .pq__nav--next:not(.is-off) { animation: pqNudge 1.8s ease-in-out infinite; }
    @keyframes pqNudge {
      0%, 100% { transform: translateX(0); opacity: 0.85; }
      50% { transform: translateX(3px); opacity: 1; }
    }
  }

  /* utility bill */
  .pq__bill {
    border: 1px solid var(--line-strong);
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.014);
    padding: clamp(1rem, 2.5vw, 1.6rem);
  }
  .pq__bill-head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding-bottom: var(--space-s);
    border-bottom: 1px solid var(--line);
  }
  .pq__bill-name {
    font-family: var(--font-mono);
    font-size: var(--step--1);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--body);
  }
  /* the meter: one charge at a time, swapping in as you scroll */
  .pq__bill-active {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
    margin: 0.5rem 0;
    padding: 0.4rem;
    border-radius: 6px;
    font-size: var(--step-0);
    color: var(--body);
  }
  .pq__bill-active-label { line-height: 1.3; }
  .pq__bill-active-amt {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    font-weight: 600;
    white-space: nowrap;
  }
  .pq__bill-active[data-kind="waste"] .pq__bill-active-label,
  .pq__bill-active[data-kind="waste"] .pq__bill-active-amt { color: #e0894a; }
  .pq__bill-active[data-kind="save"] .pq__bill-active-label,
  .pq__bill-active[data-kind="save"] .pq__bill-active-amt { color: var(--mint); }
  .pq__bill-active[data-kind="useful"] .pq__bill-active-amt { color: var(--gold-text); }
  .pq__bill-total {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-top: 0.5rem;
    padding-top: 0.55rem;
    border-top: 1px solid var(--line-strong);
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-size: var(--step--1);
    color: var(--body);
  }
  .pq__total {
    display: inline-block;
    font-family: var(--font-serif);
    font-size: var(--step-3);
    letter-spacing: -0.01em;
    color: var(--gold-text);
    text-transform: none;
    font-variant-numeric: tabular-nums;
    transition: color 0.5s ease;
  }
  .pq__stage[data-active="clean"] .pq__total { color: var(--mint); }

  .pq__dots { display: flex; gap: 0.55rem; margin-top: var(--space-l); justify-content: center; }
  .pq__dot {
    width: 9px; height: 9px;
    border-radius: 999px;
    border: 1px solid var(--line-strong);
    background: transparent;
    padding: 0;
    cursor: pointer;
    transition: background 0.3s ease, border-color 0.3s ease, transform 0.3s ease;
  }
  .pq__dot.is-active { background: var(--gold-text); border-color: var(--gold-text); transform: scale(1.15); }

  /* prev/next arrows flank the beer on mobile; hidden on desktop (scroll drives it) */
  .pq__glass-row { display: contents; }
  .pq__nav {
    display: none;
    flex: 0 0 auto;
    width: 40px;
    height: 40px;
    border-radius: 999px;
    border: 1px solid var(--gold-text);
    background: rgba(12, 26, 20, 0.72);
    color: var(--gold-text);
    font-size: 1.6rem;
    line-height: 1;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }
  .pq__dot--clean.is-active { background: var(--mint); border-color: var(--mint); }

  .pq__close {
    margin-top: var(--space-xl);
    font-family: var(--font-serif);
    font-style: italic;
    font-size: var(--step-1);
    color: var(--ink);
    max-width: 48ch;
  }

  /* MOBILE: tap / swipe to advance, NOT scroll-pinned (scroll-jacking fights
     the address bar on phones). The scene is a fixed-height panel in normal
     flow: card on top, beer in the middle filling the space, meter at the
     bottom. Cards cross-fade in place; the beer and bill never move. */
  @media (max-width: 640px) {
    .pq__track { height: auto; }
    .pq__sticky {
      position: static;
      height: 86svh;
      display: flex;
      align-items: stretch;
      padding-block: 3vh;
    }
    .pq__stage {
      display: flex;
      flex-direction: column;
      gap: var(--space-s);
      position: relative;
      width: 100%;
      box-sizing: border-box;
      /* guarantee side padding even if a flex/scroll state strips the
         container's own padding */
      padding-inline: max(env(safe-area-inset-left), 1.25rem) max(env(safe-area-inset-right), 1.25rem);
    }
    .pq__nav { display: flex; }

    /* row 1: the card box is fixed to the tallest card (grid stack from the
       base rule), so the beer and bill never move. Shorter cards center their
       text vertically inside that fixed box. Kept compact so the beer keeps
       its share of the height. */
    .pq__cards { flex: 0 0 auto; max-width: none; margin: 0; }
    .pq__card {
      padding: 0.55rem 1rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .pq__card-title { font-size: var(--step-1); }
    .pq__card-body { display: block; font-size: var(--step-0); line-height: 1.4; margin-top: var(--space-2xs); }

    /* unwrap the two columns so glass and bill are direct flex rows */
    .pq__cols { display: contents; }

    /* row 2: the beer is the hero, guaranteed a substantial share, with the
       prev/next arrows flanking it and the load label beneath it. */
    .pq__glass-col {
      flex: 1 1 auto;
      min-height: 38vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 0.4rem;
    }
    .pq__glass-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.6rem;
      flex: 1 1 auto;
      min-height: 0;
      width: 100%;
    }
    .pq__glass { width: auto; max-width: 50vw; max-height: 100%; }
    /* hidden but space reserved, so the glass stays centered card to card */
    .pq__nav.is-off { visibility: hidden; animation: none; }
    .pq__load {
      position: static;
      transform: none;
      font-size: var(--step--2);
      padding: 0.22em 0.75em;
    }

    /* row 3: the meter, content-sized */
    .pq__bill-col { flex: 0 0 auto; width: 100%; }
    .pq__bill { width: 100%; max-width: 24rem; margin-inline: auto; padding: 0.7rem 0.9rem; }
    .pq__bill-head { padding-bottom: 0.4rem; }
    .pq__bill-active { margin: 0.3rem 0; padding: 0.3rem 0.35rem; }
    .pq__bill-total { margin-top: 0.3rem; padding-top: 0.45rem; }
    .pq__total { font-size: var(--step-2); }

    .pq__title { font-size: var(--step-2); }

    /* the whole scene is tappable; dots are progress + jump targets */
    .pq__stage.is-tap { cursor: pointer; }
    .pq__dots { display: flex; margin-top: var(--space-2xs); gap: 0.7rem; }
    .pq__dot { width: 11px; height: 11px; }
  }
</style>
